" Add ruler to bottom right
set ruler

" Highlight search results
set hlsearch

" Allow regex
set magic

" No annoying sound on errors
set noerrorbells

" Enable syntax highlighting
syntax enable

" Use spaces instead of tabs
set expandtab

" 1 tab == 4 spaces
set shiftwidth=4
set tabstop=4

" Always show the status line
set laststatus=2
