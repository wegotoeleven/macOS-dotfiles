# Set Variables
currentShell=$(echo $(sh -c 'ps -p $$ -o ppid=' | xargs ps -o comm= -p | tr -d '-'))

# Prompt
PS1='%B%F{green}%n@%m%f%b %B%F{blue}%c%f%b %F{245}(${currentShell})%f %% '
#    <Show host and user> <show directory> <---Show current shell---> <%>